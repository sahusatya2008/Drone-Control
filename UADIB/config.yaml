server:
  host: 0.0.0.0
  port: 8080
security:
  token_env: UADIB_AUTH_TOKEN
protocols:
  enabled: [mavlink, ros2, sdk, telemetry_stream]
telemetry:
  stream_interval_s: 0.5
mission:
  default_speed_mps: 5.0
